import { BetterTestDirective } from './better-test.directive';

describe('BetterTestDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterTestDirective();
    expect(directive).toBeTruthy();
  });
});
